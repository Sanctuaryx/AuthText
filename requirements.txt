# Core
numpy>=1.26
pandas>=2.2
scikit_learn>=1.4
scipy>=1.11
tqdm>=4.66
joblib>=1.3
streamlit==1.53.1

# Deep Learning
# Recomendado: torch >= 2.6 por compatibilidad/seguridad reciente.
# En GPU (Windows), instala torch/torchvision/torchaudio desde la web oficial de PyTorch.
torch>=2.6,<3.0

# Transformers / NLP
transformers>=4.45,<5.0
tokenizers>=0.19
safetensors>=0.4

# Word2Vec
gensim>=4.3

# API / HTTP
Flask>=3.0
requests>=2.31

# Config
pyyaml>=6.0

# Server
gunicorn>=21.2; platform_system!="Windows"
waitress>=2.1; platform_system=="Windows"
